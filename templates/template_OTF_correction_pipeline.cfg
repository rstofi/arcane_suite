# Template otfms pipeline config file generated by arcane_suite at 2023-10-21 22:14:37.018631

[ENV]
working_dir                   =              #Mandatory, absolute path
log_level                     = INFO         #Optional, with values of the python module logging (e.g. INFO, DEBUG.. etc.)
snakemake_alias               = snakemake    #Optional, string
casa_alias                    = casa         #Optional, string
chgcentre_alias               = chgcentre    #Optional, string

[DATA]
MS                            =              #Mandatory, absolute path
pointing_ref                  =              #Mandatory, absolute path
target_field_list             =              #Mandatory, comma separated list
split_calibrators             = False        #Mandatory, boolean
calibrator_list               =              #Optional, comma separated list or none
flag_noise_diode              = False        #Mandatory, boolean
noise_diode_setup             =              #Optional, absolute path
scans                         =              #Optional, comma separated list of scan IDs or none
timerange                     =              #Optional, CASA-style timerange or none
ant1_ID                       = 0            #Optional, int or none
ant2_ID                       = 1            #Optional, int or none
time_crossmatch_threshold     = 0.001        #Optional, float or none
split_timedelta               = 0.5          #Optional, float or none
position_crossmatch_threshold = 0.0025       #Optional, float or none

[OUTPUT]
OTF_acronym                   = otfms        #Mandatory, string
skip_merge                    = False        #Mandatory, boolean
MS_outname                    = final        #Mandatory, string (without the .ms extension, not needed if `skip_merge` is True)
deep_clean                    = False        #Optional, boolean
